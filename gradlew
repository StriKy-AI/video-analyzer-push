#!/usr/bin/env sh

#
# Standard Gradle wrapper script (excerpt).
# GitHub Actions will generate the full gradlew/gradlew.bat + gradle-wrapper.jar
# via the `gradle/actions/setup-gradle` action, so we don't ship the JAR in git.
#